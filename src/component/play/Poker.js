import React from 'react';
import {Button,ButtonToolbar} from 'react-bootstrap';
const Poker = (props) => {
    const {user} = props;
    return (
        <div>


        </div>
    );
};

export default Poker;